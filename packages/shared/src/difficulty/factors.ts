import type { AnalyzeNote } from './types'

/** Stub — implemented in Task 2 */
export function computeNpsOverTime(
  _notes: AnalyzeNote[], _windowSeconds = 2, _resolution = 0.5, _maxTime = 300,
): Array<{ time: number; nps: number }> {
  return []
}

export function maxCombo(_notes: AnalyzeNote[]): number {
  return 0
}
